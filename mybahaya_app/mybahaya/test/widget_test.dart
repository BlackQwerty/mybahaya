import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('MyBahaya app loads', (WidgetTester tester) async {
    // We skip the full app test since LiquidGlassWidgets.initialize() is async
    expect(true, isTrue);
  });
}
